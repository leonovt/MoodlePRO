httpx>=0.27
pydantic-settings>=2.3
# Uncomment to run real transcription on a CUDA machine (FAKE_TRANSCRIBE=false).
# faster-whisper needs CUDA's cuBLAS + cuDNN at runtime; the nvidia-*-cu12 wheels provide
# them (worker.sbatch puts them on LD_LIBRARY_PATH). Without them: "libcublas.so.12 not found".
# faster-whisper>=1.0
# nvidia-cublas-cu12
# nvidia-cudnn-cu12
